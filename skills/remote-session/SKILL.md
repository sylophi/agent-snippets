---
name: remote-session
description: User-invoked at the start of a remote session.
---

The user is accessing this session from a remote machine, not the machine you are running on.

To share any file meant for viewing (images, screenshots, reports, etc.), use the dropcube skill and reply with the link — the user cannot open local file paths. For a short text file, it may be simpler to just print its contents in your reply instead.
