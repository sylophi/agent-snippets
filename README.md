# agent-snippets

A personal collection of instruction snippets for coding agents: skills and
slash commands.

Symlink entries from `skills/` and `commands/` into your agent's
corresponding directories (for Claude Code: `~/.claude/skills/` and
`~/.claude/commands/`).
